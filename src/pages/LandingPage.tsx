import { Box, Typography } from '@mui/material';

export const LandingPage = () => (
  <Box sx={{ p: 4 }}>
    <Typography variant="h2">Bem-vindo!</Typography>
    <Typography>A landing page será desenvolvida aqui.</Typography>
  </Box>
);